#include <stdio.h>
/**
 * main - Entry point
 * Program uses puts instead of printf for text output
 */
int main(void)
{
  char Mystr[] = "\"Programming is like building a multilingual puzzle";

  puts(Mystr);

  return (0);
}
